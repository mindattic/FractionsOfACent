{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "FractionsOfACent": {
    "DbPath": "../../findings.db",
    "_DbPathNote": "Path is relative to the working directory the web app is launched from. Default points at the repo-root findings.db.",
    "_NoticeOverride": "Leave NoticeChannel/NoticeTitle/NoticeBody unset to use the in-code defaults from NoticeService.cs (recommended). Override only if you want a different template than the default."
  }
}
